---
description: Create a new PRD for a feature
agent: build
---

Load the `prd` skill and create a PRD for the following feature:

$ARGUMENTS

## Instructions

1. First, create the `tasks/` directory if it doesn't exist
2. Follow the prd skill to ask 3-5 clarifying questions with lettered options
3. Wait for answers before generating the PRD
4. Generate a structured PRD based on the answers
5. Save to `tasks/prd-[feature-name].md`
6. Confirm the file was created

Remember: Each user story must be small enough to complete in ONE context window. Split large stories into smaller ones.
